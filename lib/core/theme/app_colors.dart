import 'package:flutter/material.dart';

class AppColors {
  // Cores primárias
  static const Color primary = Color(0xFF1E3A8A); // Azul escuro/petróleo
  static const Color secondary = Color(0xFF3B82F6); // Azul médio (ao invés de laranja)
  
  // Cores de fundo
  static const Color background = Color(0xFFFFFFFF); // Branco
  static const Color surface = Color(0xFFF8F9FA); // Cinza muito claro
  static const Color surfaceDark = Color(0xFFE9ECEF); // Cinza claro
  
  // Cores de texto
  static const Color textPrimary = Color(0xFF1F2937); // Cinza escuro
  static const Color textSecondary = Color(0xFF6B7280); // Cinza médio
  
  // Cores de estado
  static const Color error = Color(0xFFDC2626);
  static const Color success = Color(0xFF059669);
  static const Color warning = Color(0xFFF59E0B);
  
  // Cores auxiliares
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color disabled = Color(0xFF9CA3AF);
  
  // Variações da cor primária
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF1E293B);
  
  // Variações do azul secundário
  static const Color secondaryLight = Color(0xFF60A5FA);
  static const Color secondaryDark = Color(0xFF2563EB);
}
